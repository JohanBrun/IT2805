1. This is due to innacurracy in floation point calculation. If we log the result of 0.1 + 0.2 we get 0.30000000000000004 which is not equal to 0.3.

2. The method attribute decides how the form-data is sent.

3. The get method is used for the browser to make a request to the server. 

4. Form validation using JavaScript can be done as users type and thus give a responsive and intuitive validation.
However, if the user turns of JavaScript then the validation is lost without the HTML5 validation.

5. When we want the user to enter multiple lines of text.s